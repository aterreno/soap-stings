- api list on homepage? auto-gen?
- config where? clearly in files it's not being good
- more endpoint
- what's the end goal? commandline/curl call from Docker ENV?
- Docker file & possibly docker zeus
- handle 404 & errors
